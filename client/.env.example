# No environment variables needed for production - client and server
# are deployed together on the same domain.
#
# For local development, the Vite dev server proxies /api requests
# to http://localhost:5000 (configured in vite.config.ts).
